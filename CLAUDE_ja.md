# CLAUDE.md - React Todoアプリ

## プロジェクト概要

これはReactベースのTodoアプリケーションです。このドキュメントは、このコードベースで作業するAIアシスタント（Claudeなど）のためのガイドとして、プロジェクト構造、開発ワークフロー、主要な規約についての情報を提供します。

## リポジトリの状態

**現在の状態**: 開発中のReact Todoアプリケーション

**プロジェクトタイプ**: React Todoアプリケーション

## 技術スタック

- **React**: UIライブラリ (React 18+)
- **JavaScript/TypeScript**: プログラミング言語
- **Vite**: ビルドツール
- **CSS/UIライブラリ**: スタイリング
- **localStorage**: データ永続化
- **ESLint**: コード品質チェック

## プロジェクト構造

```
react-todo-app/
├── public/                 # 静的アセット
│   └── vite.svg           # アイコン
├── src/                   # ソースコード
│   ├── components/        # Reactコンポーネント
│   │   ├── TodoList.jsx   # Todoリストコンポーネント
│   │   ├── TodoItem.jsx   # 個別のTodoアイテム
│   │   ├── AddTodo.jsx    # Todo追加フォーム
│   │   └── Filter.jsx     # フィルターコントロール
│   ├── hooks/             # カスタムReactフック
│   │   └── useTodos.js    # Todo状態管理ロジック
│   ├── utils/             # ユーティリティ関数
│   │   └── storage.js     # localStorage操作
│   ├── styles/            # スタイルファイル
│   │   └── App.css        # グローバルスタイル
│   ├── App.jsx            # メインアプリコンポーネント
│   └── main.jsx           # エントリーポイント
├── package.json           # 依存関係とスクリプト
├── vite.config.js         # Vite設定
├── .gitignore            # Git除外ルール
├── README.md             # プロジェクトドキュメント
├── CLAUDE.md             # 英語版ガイド
└── CLAUDE_ja.md          # このファイル（日本語版ガイド）
```

## 開発ワークフロー

### 初期セットアップ

プロジェクトを初めてセットアップする場合：

1. **依存関係のインストール**:
   ```bash
   npm install
   ```

2. **開発サーバーの起動**:
   ```bash
   npm run dev
   ```

3. **ブラウザでアクセス**:
   通常は `http://localhost:5173` でアクセスできます

### 日常的な開発ワークフロー

1. **最新の変更を取得**:
   ```bash
   git pull origin main
   ```

2. **機能ブランチを作成**（必要に応じて）:
   ```bash
   git checkout -b feature/機能名
   ```

3. **変更を加えてローカルでテスト**

4. **リントチェック**:
   ```bash
   npm run lint
   ```

5. **変更をコミット**:
   ```bash
   git add .
   git commit -m "feat: わかりやすいコミットメッセージ"
   ```

6. **リモートにプッシュ**:
   ```bash
   git push -u origin ブランチ名
   ```

### プロダクションビルド

```bash
npm run build
```

## AIアシスタントのための主要な規約

### コードスタイル

1. **コンポーネント構造**:
   - フックを使用した関数コンポーネントを使用
   - コンポーネントは小さく、単一責任を保つ
   - 関連するファイルを同じ場所に配置（コンポーネント + スタイル + テスト）

2. **命名規則**:
   - **コンポーネント**: PascalCase（例: `TodoList.jsx`, `AddTodo.jsx`）
   - **ファイル**: コンポーネント名と同じ
   - **関数**: camelCase（例: `addTodo`, `removeTodo`）
   - **定数**: UPPER_SNAKE_CASE（例: `MAX_TODO_LENGTH`）
   - **CSSクラス**: kebab-case

3. **ファイル構成**:
   - 機能ごとにグループ化（プロジェクトが大きくなったら）
   - 関連ロジックを一緒に保つ
   - 必要に応じてindexファイルを使用

### Reactのベストプラクティス

1. **状態管理**:
   - ローカルステート（useState）から始める
   - 必要に応じて状態を持ち上げる
   - 深いプロップドリリングにはContextを使用
   - 複雑な状態ロジックにはuseReducerを検討

2. **Props**:
   - 明確にするためにpropsを分割代入
   - 型チェックにはPropTypesまたはTypeScriptを使用
   - プロップドリリングは最大2-3レベルまで

3. **副作用**:
   - useEffectを副作用に使用
   - 常に依存関係配列を指定
   - 必要に応じてエフェクトをクリーンアップ（cleanup関数を返す）

4. **パフォーマンス**:
   - React.memo()は必要な場合のみ控えめに使用
   - 高価な計算はuseMemoでメモ化
   - メモ化された子コンポーネントに渡すコールバックはuseCallbackでメモ化

### Todoアプリ固有の規約

1. **Todoデータ構造**:
   ```javascript
   {
     id: string | number,        // 一意の識別子（UUIDまたはタイムスタンプ）
     text: string,                // Todo の説明
     status: string,              // ステータス: 'not-started' | 'in-progress' | 'completed' | 'on-hold'
     createdAt: Date | string,   // 作成日時
     updatedAt?: Date | string   // 最終更新日時（オプション）
   }
   ```

2. **ステータスの種類**:
   - **not-started** (未完了): まだ開始していないタスク
   - **in-progress** (進行中): 現在作業中のタスク
   - **completed** (完了): 完了したタスク
   - **on-hold** (保留中): 一時的に保留されているタスク

3. **コア機能**:
   - 新しいTodoの追加
   - Todoのステータス変更（4つのステータス間で切り替え）
   - Todoの削除
   - 既存のTodoの編集
   - Todoのフィルタリング（全て/未完了/進行中/完了/保留中）
   - localStorageへの永続化
   - Todoカウントの表示

4. **データ永続化**:
   - localStorageを使用して永続化
   - 変更ごとに保存
   - アプリ初期化時にロード
   - JSONパースエラーを適切に処理

### Git規約

1. **ブランチ命名**:
   - 機能: `feature/説明` または `feat/説明`
   - バグ修正: `fix/説明` または `bugfix/説明`
   - Claude AIブランチ: `claude/セッションID`（自動生成）

2. **コミットメッセージ**（Conventional Commits）:
   - `feat:` - 新機能
   - `fix:` - バグ修正
   - `docs:` - ドキュメント変更
   - `style:` - コードスタイル変更（フォーマット、ロジック変更なし）
   - `refactor:` - コードリファクタリング
   - `test:` - テストの追加または更新
   - `chore:` - メンテナンスタスク

   例: `feat: Todoのフィルタリング機能を追加`

3. **プルリクエスト**:
   - 変更の明確な説明を提供
   - 関連する問題を参照
   - レビューをリクエストする前にテストが通ることを確認

### コメント規約（初心者向け）

1. **コンポーネントの説明**:
   ```javascript
   /**
    * TodoItem - 個別のTodoアイテムを表示するコンポーネント
    * @param {Object} todo - Todoオブジェクト
    * @param {Function} onStatusChange - ステータス変更時のコールバック
    * @param {Function} onDelete - 削除時のコールバック
    */
   ```

2. **複雑なロジックの説明**:
   ```javascript
   // localStorageからTodoリストを取得し、JSON形式でパースする
   // エラーが発生した場合は空の配列を返す
   ```

3. **状態管理の説明**:
   ```javascript
   // todos: 全てのTodoアイテムを保持する配列
   const [todos, setTodos] = useState([]);
   ```

### アクセシビリティ（a11y）

1. **セマンティックHTML**:
   - 適切なHTML要素を使用（`<button>`, `<input>`, `<label>`）
   - インタラクティブ要素に`<div>`を避ける

2. **ARIA属性**:
   - アイコンボタンにaria-labelを追加
   - カスタムチェックボックスにaria-checkedを使用
   - 動的更新にaria-liveリージョンを含める

3. **キーボードナビゲーション**:
   - すべてのインタラクティブ要素がキーボードでアクセス可能
   - 論理的なタブ順序
   - Enter/Spaceでボタンをアクティブ化

### エラーハンドリング

1. **ユーザー入力の検証**:
   - 空のTodoを許可しない
   - 空白をトリミング
   - 最大長制限を検討

2. **localStorageエラー**:
   - try-catchブロックでラップ
   - localStorageが利用できない場合のフォールバックを提供
   - クォータ超過エラーを処理

3. **適切な劣化**:
   - localStorageが失敗してもアプリは動作するべき
   - ユーザーフレンドリーなエラーメッセージを表示

## AIアシスタントとの作業

### 変更を加える時

1. **変更前に必ずファイルを読む**
2. **既存のパターンを理解する**
3. **既存のコードスタイルとの一貫性を保つ**
4. **過度にエンジニアリングしない** - シンプルに保つ
5. **新機能にはテストを書く**
6. **機能追加時はドキュメントを更新**

### コミット前

1. **アプリが動作することを確認**: `npm run dev`
2. **リントエラーをチェック**: `npm run lint`
3. **ブラウザでテスト** - 実際にアプリを使用
4. **変更を確認**: `git diff`

### 困った時

1. package.jsonで利用可能なスクリプトを確認
2. 既存の類似コンポーネントやパターンを探す
3. コンソールでエラーを確認
4. React DevToolsでコンポーネント階層と状態を確認
5. ブラウザDevToolsでネットワーク/ストレージの問題を確認

## よくあるタスク

### 新機能の追加

1. `src/components/`に新しいコンポーネントファイルを作成
2. コンポーネントロジックとJSXを書く
3. スタイリングを追加
4. 親コンポーネントでインポートして使用
5. テストを書く
6. 必要に応じてこのドキュメントを更新

### バグ修正

1. バグを再現
2. 問題のあるコードを特定
3. バグを露出するテストを書く（TDDアプローチ）
4. バグを修正
5. テストが通ることを確認
6. アプリが正しく動作することを確認

### リファクタリング

1. テストが存在し、通ることを確認
2. リファクタリング変更を行う
3. すべてのテストがまだ通ることを確認
4. アプリの動作が変わっていないことを確認
5. 影響を受けるドキュメントを更新

## パフォーマンス考慮事項

1. **再レンダリングの最適化**:
   - レンダリング内で関数/オブジェクトを作成しない
   - 最適化された子コンポーネントに渡す時はメモ化
   - リストでkey propを正しく使用

2. **リスト仮想化**:
   - 100+のTodoには検討
   - react-windowのようなライブラリを使用

3. **バンドルサイズ**:
   - ビルドアナライザーでモニター
   - 必要に応じてコンポーネントを遅延ロード
   - 未使用の依存関係をツリーシェイク

## セキュリティ考慮事項

1. **XSS防止**:
   - Reactはデフォルトでエスケープ - dangerouslySetInnerHTMLを使用しない
   - 必要に応じてユーザー入力をサニタイズ

2. **依存関係**:
   - 依存関係を最新に保つ
   - `npm audit`を定期的に実行
   - セキュリティアドバイザリを確認

## 将来の機能拡張

検討すべき機能：

- 期限とリマインダー
- カテゴリ/タグ
- 優先度レベル
- 検索機能
- ドラッグ&ドロップで並べ替え
- 繰り返しTodo
- エクスポート/インポート機能
- クラウド同期（バックエンド必要）
- コラボレーション機能（バックエンド必要）
- ダークモード
- モバイルレスポンシブ
- オフライン使用のためのPWAサポート

## 追加リソース

- [React ドキュメント](https://ja.react.dev)
- [React Testing Library](https://testing-library.com/react)
- [JavaScript ベストプラクティス](https://developer.mozilla.org/ja/docs/Web/JavaScript/Guide)
- [Conventional Commits](https://www.conventionalcommits.org/ja/)
- [Webアクセシビリティガイドライン](https://waic.jp/docs/WCAG21/)

## 質問や問題？

このプロジェクトで作業する際：
- リポジトリの既存のissueを確認
- 規約についてはこのドキュメントを確認
- パターンについては既存のコードを見る
- 仮定が不明確な場合は明確化を求める

---

**最終更新**: 2026-01-19
**ステータス**: 開発中
